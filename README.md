# Taksha Engineering — Website

One-page Next.js (App Router) site scaffold built with TypeScript and Tailwind CSS.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Netlify

- This repo includes `netlify.toml` configured to use `@netlify/plugin-nextjs`.
- On Netlify, set the build command to `npm run build`.
